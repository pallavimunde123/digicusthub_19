server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/digicusthub2025?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: DIGI-CUST-SERVICE
